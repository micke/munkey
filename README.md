# README

This is a private project for a discord bot with accompanying Rails control
panel.

The bot mostly lives in `lib/bot`

# Docker

A docker image is published to dockerhub as `lisinge/mkswe-discord`
